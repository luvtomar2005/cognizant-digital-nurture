# Week 4 - Spring REST using Spring Boot

## Exercise 1: Create a Spring Web Project using Maven

### Objective

Create a Spring Boot Web project using Maven and understand the basic project structure generated by Spring Initializr.

This exercise serves as the foundation for all upcoming Spring REST hands-on exercises.

---

## Project Information

| Property | Value |
|----------|-------|
| Group ID | com.cognizant |
| Artifact ID | spring-learn |
| Build Tool | Maven |
| Language | Java |
| Packaging | JAR |
| Dependencies | Spring Web, Spring Boot DevTools |

---

## Technologies Used

- Java
- Spring Boot
- Spring Web
- Spring Boot DevTools
- Maven

---

## Project Structure

```
spring-learn
│
├── src
│   ├── main
│   │   ├── java
│   │   └── resources
│   │
│   └── test
│       └── java
│
├── pom.xml
├── mvnw
├── mvnw.cmd
└── HELP.md
```

---

## Important Components

### `src/main/java`

Contains the application's source code.

### `src/main/resources`

Contains configuration files such as:

- application.properties
- static resources
- templates

### `src/test/java`

Contains unit and integration test classes.

### `SpringLearnApplication.java`

Entry point of the Spring Boot application.

```java
@SpringBootApplication
public class SpringLearnApplication {

    public static void main(String[] args) {
        SpringApplication.run(SpringLearnApplication.class, args);
    }

}
```

---

## @SpringBootApplication

This annotation combines:

- `@SpringBootConfiguration`
- `@EnableAutoConfiguration`
- `@ComponentScan`

It enables auto-configuration, component scanning, and bootstraps the Spring Boot application.

---

## Build the Project

Using Maven Wrapper:

```bash
./mvnw clean package
```

Windows:

```cmd
mvnw.cmd clean package
```

---

## Run the Application

Using Maven Wrapper:

```bash
./mvnw spring-boot:run
```

Windows:

```cmd
mvnw.cmd spring-boot:run
```

---

## Output

Application starts successfully with embedded Tomcat.

Example log:

```
Tomcat started on port 8080 (http)

Started SpringLearnApplication
```

---

## Verification

Visit:

```
http://localhost:8080
```

Since no REST controller or static page has been created yet, Spring Boot returns:

- HTTP Status 404 (Not Found)

This is the expected behavior for this exercise.

---

## Learning Outcomes

- Created a Spring Boot Web project using Spring Initializr.
- Understood the Maven project structure.
- Explored the purpose of `src/main/java`, `src/main/resources`, and `src/test/java`.
- Learned the role of `SpringLearnApplication`.
- Understood the purpose of `@SpringBootApplication`.
- Built the project using Maven.
- Successfully started the embedded Tomcat server.
- Verified the application is running correctly.

---

## Status

**Exercise Completed Successfully**